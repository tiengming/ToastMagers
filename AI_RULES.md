# AGENTS.md — ToastMagers 项目 AI 开发规则

本文件面向在本仓库上执行任务的编码 Agent（Jules 及其他遵循 AGENTS.md 开放标准的 Agent），用于约束其在文件写入、版本更新、开发流程与安全审核方面的行为。人工在对话/任务描述中给出的显式指令优先于本文件；本文件用于设定未被显式指令覆盖时的默认行为基线。

## 参考文档

- `README.md`：项目设计文档，包含技术方案、配置结构、已知限制与生产级工程要求，任何实现须与其保持一致
- `TASKS.md`：任务单与优先级、依赖关系定义，任务执行前须先核对对应任务编号的前置依赖与验收标准

## 项目概览

ToastMagers 是面向 Magisk / KernelSU / APatch 的系统级 Android 通知与 Toast 拦截模块，当前处于设计与早期开发阶段，尚无最终代码骨架。在骨架代码建立前，涉及具体文件路径、语言/框架选型的规则以 `README.md`〈开发状态〉一节为准；本文件中的目录与工具链约定为规划依据，随实际仓库结构确立需同步修正。

## 环境与构建

- 计划工具链：Android NDK r25c 及以上、CMake 3.22.1 及以上、Gradle 8.0 及以上
- 计划构建命令：`./gradlew zipModule`，产物路径 `out/ToastMagers-release.zip`
- 上述命令用于 Jules 项目「Configuration → Environment」中的环境搭建脚本配置，AGENTS.md 本身不会被当作可执行脚本运行；脚本内容需在实际仓库结构确定后于 Jules 控制台中验证（Run to Validate）后固化
- 环境变量、访问凭据等敏感配置一律通过 Jules 仓库级环境变量注入，不写入仓库任何文件

## 开发规则

- 每个开发任务须对应 `TASKS.md` 中的任务编号（如 `T-HOOK-03`），PR 描述与 commit message 中须注明该编号
- 严禁跳过 P0 预研任务（`T-HOOK-01`、`T-HOOK-02`、`T-RULE-01`、`T-SYNC-01`）直接开始对应模块的正式开发；若相关预研任务尚无结论，对应开发任务应保持阻塞，并在 PR/Issue 中说明阻塞原因
- 涉及 Hook 回调（Epic A）的任何改动，必须保证 Fail-open 语义：新增或修改的 Hook 回调须有异常捕获且默认放行原始调用；不满足该条件的改动不得合并
- 涉及正则规则解析（Epic B）的改动，必须使用 `T-RULE-01` 预研阶段确定的正则引擎与复杂度限制方案，不得引入未经复杂度评估的自由格式正则匹配逻辑
- 涉及系统权限状态同步（Epic F）的改动，须确认 `T-SYNC-01` 的可行性结论已产出且明确可行后方可实现 `T-SYNC-02`

## 文件写入与目录规则

- 新增源码文件应按 README 中划分的功能模块归类（Hook 引擎、规则引擎、应用扫描、配置系统、WebUI），具体目录结构在骨架代码建立时于本文件中补充固化
- 严禁在仓库中提交任何明文密钥、签名证书私钥或访问令牌；相关内容一律通过 Jules 仓库级环境变量注入
- 不得直接修改构建产物（如 `out/` 目录内容），产物目录不纳入版本控制
- 配置文件示例（如 `config.json` 模板）须与用户实际配置区分，示例文件建议以 `.example` 或 `.sample` 结尾，避免被误认为默认生效配置

## 更新与版本规则

- 遵循语义化版本号（SemVer），任何影响对外行为的改动须在 `CHANGELOG.md` 中记录条目，规范与 `TASKS.md` 中 `T-REL-01` 保持一致
- 已发布版本对应的 Git Tag 不得回溯修改；修复类改动须以新版本号发布
- `README.md` / `TASKS.md` 中标注的【待实现】条目，对应功能完成后须在同一 PR 内同步更新或移除该标注，避免文档与代码状态脱节

## 测试与验证规则

- 涉及 Hook 引擎、规则引擎、系统权限同步（Epic A/B/F）的改动，PR 中必须包含可复现的验证记录（日志片段、测试执行结果等），不接受仅有代码改动而无验证说明的提交
- 引入或修改正则规则解析逻辑时，须附带针对已知病态正则样本的回归测试结果（对应 `T-SEC-03`）
- 自动化测试框架建立前，PR 描述中须明确列出已完成的人工/脚本化验证步骤，不得仅以「代码已完成」作为任务结束依据

## 安全与人工审核规则

- 本仓库对应受保护分支应禁用自动合并（auto-merge），此项需由仓库维护者在 GitHub 分支保护设置中配置，不属于本文件可控制范围，但所有 Agent 生成的 PR 均须视为待人工审阅状态，不得自行判定为可直接合并
- 涉及 Epic A（Hook 引擎/进程注入）与 Epic F（系统权限状态同步）的改动，风险等级为系统级（可能影响 `system_server` 稳定性），此类 PR 须在描述中标注风险等级，等待明确人工批准后方可合并
- 仓库不应包含任何明文密钥；如任务需要访问密钥/凭据，一律通过 Jules 仓库级环境变量配置

## 文档同步规则

- 任何改变 `README.md` / `TASKS.md` 中已描述设计、接口或行为的改动，须在同一 PR 内同步更新对应文档段落
- 新实现的验收标准如与 `TASKS.md` 中已定义标准存在差异，须在 PR 描述中说明差异原因，不得静默偏离原验收标准
