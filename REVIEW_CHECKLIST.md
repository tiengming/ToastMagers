# PR 审查标准（任务1/2/3）

本清单用于审查 Jules 针对以下三个任务提交的 PR，逐项对照 README.md / TASKS.md / AGENTS.md 中已定义的规则。任一「阻断项」不满足即不得合并，需退回重做。

---

## 一、通用检查项（三份 PR 均适用）

| 检查项 | 标准 | 级别 |
| --- | --- | --- |
| 任务编号引用 | PR 描述/commit message 中明确列出对应的 TASKS.md 编号（如 T-HOOK-02） | 阻断 |
| 构建可用 | `./gradlew build`（或对应命令）本地/CI 执行成功，非"仅在 Jules 环境里跑通" | 阻断 |
| 无明文密钥 | 全文搜索 `password`、`secret`、`token`、`api_key`、`.pem`、`BEGIN PRIVATE KEY` 等关键词，确认无泄露 | 阻断 |
| 构建产物未入库 | `out/`、`build/` 等产物目录未被提交 | 阻断 |
| 目录归属正确 | 新增文件落在 README/TASKS 划分的对应模块目录下，未出现跨模块散乱写入 | 提醒 |
| 文档同步 | 若实现内容与 README.md/TASKS.md 已有描述不一致，PR 是否同步更新了对应文档段落，并说明差异原因 | 阻断 |
| 【待实现】标注处理 | 完成的功能对应的【待实现】标注是否已在文档中更新/移除；未完成部分是否仍保留标注 | 阻断 |
| 诚实性核查（最重要） | PR 描述是否如实说明"仅完成代码/可编译/mock 验证"，还是把这类结果包装成"已完成"或"已验证"——这是本轮审查的核心风险点，需要逐条对照下方分任务清单核实，不能只看 PR 标题 | 阻断 |

---

## 二、任务1（骨架搭建）专项检查

| 检查项 | 标准 | 级别 |
| --- | --- | --- |
| Epic 目录对齐 | 是否为 Hook 引擎 / 规则引擎 / 应用扫描 / 配置系统 / WebUI / 权限同步 / 安全 / 性能 / 测试 / 发布工程建立了对应目录，命名与 README 一致 | 阻断 |
| Magisk 模块骨架 | `module.prop`、`META-INF/com/google/android/update-binary`、`updater-script` 等 systemless 挂载所需文件齐全 | 阻断 |
| 打包任务可运行 | `./gradlew zipModule` 能执行完成并在 `out/` 生成 zip（内容可以是空壳，但命令本身不能报错） | 阻断 |
| 抽象接口无真实逻辑 | `SystemHookBridge`、`NotificationServiceAccessor` 等接口是否只有签名/占位实现，未混入真实系统调用代码——若骨架阶段就写了真实 Hook 逻辑，说明 Jules 越过了任务边界 | 阻断 |
| CI 配置真实可用 | 触发一次 CI，确认真正执行了构建/测试步骤，而非空壳 workflow 文件 | 提醒 |

## 三、任务2（业务逻辑 + mock 测试）专项检查

| 检查项 | 标准 | 级别 |
| --- | --- | --- |
| 正则引擎选型来源 | **重点核实**：T-RULE-01（正则引擎/ReDoS 选型预研）实际尚未独立完成，任务2的规则引擎实现所用的正则方案，PR 中是否老实说明"这是为完成任务临时选定、非正式预研结论，待 T-RULE-01 补做后可能调整"，而不是默默把自己的选择包装成既定结论 | 阻断 |
| Fail-open 语义保留 | 即便是 mock 实现，Hook 相关代码路径是否仍然保留 try-catch + 默认放行结构，为后续替换真实实现打好安全基础 | 阻断 |
| ReDoS 回归测试真实性 | T-SEC-03 对应的测试用例，是否使用了真实的病态正则样本（如嵌套量词 `(a+)+$`）并验证超时/复杂度上限生效，而非仅测试正常输入 | 阻断 |
| 配置解析健壮性 | T-CFG-02 的畸形输入测试是否覆盖类型错误、字段缺失、超大文件等场景，而非只测试合法配置 | 提醒 |
| 真机依赖任务的边界声明 | T-HOOK-03～06、T-SCAN-02、T-SYNC-01～02 等依赖 system_server 环境的任务，PR 中是否**明确列出**"仅完成代码与 mock 单元测试，未满足 TASKS.md 原定真机验收标准"，未被标记为已完成状态（例如 TASKS.md 或 issue 状态里不能显示为 closed/done） | 阻断 |

## 四、任务3（WebUI mock）专项检查

| 检查项 | 标准 | 级别 |
| --- | --- | --- |
| 浏览器直接可用 | `webroot/index.html` 能在普通浏览器直接打开预览，不依赖 Root 管理器 WebView 环境 | 阻断 |
| JS 桥接统一封装 | mock 桥接函数是否集中在单一文件（如 `bridge.js`），后续替换为真实实现时只需替换这一个文件，未散落在多处 | 阻断 |
| 隐私工程约束 | Toast 溯源日志界面默认是否不展示通知正文，仅展示来源包名与时间戳 | 阻断 |
| mock 数据标注 | 界面中使用的示例/mock 数据是否有明显标注（如注释或界面提示），避免被误认为真实数据源 | 提醒 |
| T-WEB-01 范围界定 | PR 是否清楚说明三方案（Magisk/KernelSU/APatch）真机兼容性验证不在本任务范围内，未被误标为已验证 | 阻断 |

---

## 五、红线检查（决定能否合并的最终判断）

以下任一情况出现，**直接打回，不进入常规 code review 流程**：

1. 任何 P0 预研任务（T-HOOK-01、T-HOOK-02、T-RULE-01、T-SYNC-01）的结论被当作既定事实在实现中直接使用，而预研本身并未真正完成（即 Jules 编造了一个看似合理的技术假设去填补预研空白）
2. 真机验证类任务被标记为「已完成」而非「已完成代码与 mock 验证，待真机确认」
3. 涉及 Epic A（Hook 引擎）或 Epic F（权限同步）的改动未经人工标注风险等级、且未等待人工批准即被提交为可合并状态
4. PR 描述与实际代码改动内容明显不符（如声称实现了某功能，代码中实际是空函数/TODO）

---

## 六、审查记录模板

审查每个 PR 时建议按以下格式记录结论，便于追溯：

```
PR #___  对应任务：T-___
[ ] 通用检查项全部通过
[ ] 专项检查项全部通过
[ ] 红线检查：无触发项
结论：□ 批准合并  □ 需修改后重审  □ 打回重做
说明：
```
